<!-- Generated by tests/walkthrough/test_walkthrough.gd from tests/test_bootstrap.gd. Do not edit this file: edit the suite, run the tests, commit what they wrote. -->

# 16 — The harness and the pin

*Written by the suite from [`tests/test_bootstrap.gd`](../tests/test_bootstrap.gd). Every line under **What the suite asserts** is the name of a test that runs on every pull request, and its link is the assertion.*

M0's "CI green on an empty test" (Appendix A step 3).

The first assertion is deliberately trivial: its only job is to prove the
runner, the vendored addon and the workflow line up, so that the first red
suite at M1 is a real failure and not a plumbing problem.

The second one is not trivial. ADR-008 pins the engine and says upgrades are
their own ADR with a full export-target test pass -- so an unannounced engine
change should turn CI red, here, immediately, rather than surface later as a
physics drift nobody can date.

## What the suite asserts

- [The harness runs](../tests/test_bootstrap.gd#L16)
- [Engine matches the pin](../tests/test_bootstrap.gd#L20)
- [The pin agrees with the godot version file](../tests/test_bootstrap.gd#L40)

---

← [15 — Physics guarantees](15-physics-guarantees.md) · [Index](README.md) · [17 — How this walkthrough is made](17-how-this-walkthrough-is-made.md) →
