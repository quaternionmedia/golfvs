# golfVs — Decision Log (ADRs)

One entry per ratified decision. Ratified by the human ratifier; drafted by assistants. A `[PROPOSED]` item in `DESIGN.md` becomes an ADR here when merged. Superseded entries are kept and marked.

| ADR | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| 000 | 2026-09-03 | Adopt `DESIGN.md` 1.0-draft.1 as the plan of record | Refreshed from working drafts v0.1–v0.8; supersedes them entirely | Active |
| 001 | 2026-09-03 | Camera: free orbit, decoupled from aim; two-finger drag on touch; one-tap reset; auto putt view | Feels like golf; readability recovered via any-angle tells and mandatory audio motifs | Active |
| 002 | 2026-09-03 | Distribution: open source, free; no DLC/IAP/entitlements; GitHub releases canonical, itch.io / F-Droid mirrors | Consistent with QM; removes an entire server/receipt surface | Active |
| 003 | 2026-09-03 | Art: modeled in-house in Blender; `.blend` in repo via LFS; vertex-color pipeline; per-sport art card with idle/tell/act/react | Reproducible by contributors; placeholders and finals share one pipeline | Active |
| 004 | 2026-09-03 | Defenders are silent; personality via animation and props only; no VO, no text bubbles | No VO budget, no localization surface; tell audio motif is a gameplay signal and is unaffected | Active |
| 005 | 2026-09-03 | Licenses: Apache-2.0 (code), CC-BY-4.0 (art/audio); inbound contributions under Apache-2.0 §5, no CLA | Patent grant; permissive art reuse with attribution | Active |
| 006 | 2026-09-03 | Team: small QM team; process enforced by tooling — CODEOWNERS on `docs/` and `core/`, branch protection, CI check that DESIGN edits carry a DECISIONS entry; `HANDOFF.md` is the only cross-session memory | "Assistants draft, humans ratify" made mechanical | Active |
| 007 | 2026-09-03 | Input: touch-first; the stroke gesture is tuned on a phone; Android debug build on device is an M0/M1 exit criterion; mouse and gamepad adapt from touch | One reference feel; mobile is the hardest target | Active |
| 008 | 2026-09-03 | Godot: pin to the latest 4.x stable at M0; record exact version in `project.godot`, README, CI, and here; upgrades are their own ADR. **Pinned at M0 to `4.7.2.stable`** (`.godot-version` is the machine-readable source; CI resolves the official Linux build from it; the bootstrap suite fails if the running engine differs) | Never float on "latest"; upgrades get a full export-target test pass | Active |
| 009 | 2026-09-03 | Defense re-plans at every lie (Defense Range and Match); exactly one defender moves per lie, within its sport's repositioning range; focus zones re-settable on all defenders every lie | Bounded movement makes the hole a series of reads; unlimited is omniscient, none is static | Active |
| 010 | 2026-09-03 | All scoring beyond the per-hole scorecard — ledgers, indexes, rating-adjusted defense baseline, medals, unlock trees, Compound display, Match Elo, ladders, relay — is deferred to a post-1.0 phase. 1.0 ships with the scorecard as the sole result and everything available from first launch. The parked design is preserved in `DESIGN.md` §12. | Scope; 1.0 must prove the stroke, the defenders, records, and async play before any meta-game is layered on | Active — supersedes the interim rating-baseline and per-hole-record decisions from drafts v0.7–v0.8, which now live in §12 |
| 011 | 2026-09-03 | No dates or durations in planning documents; milestones are dependency-ordered and gate-terminated; `HANDOFF.md` records what is in progress, not when it is due | Estimates were noise before M1; gates are the real schedule | Active |
| 012 | 2026-09-09 | Record floats are **quantized on write**: positions to 0.1 mm (4 dp); normalised scalars and unit-vector components to 6 dp. The quantized value is what the simulation consumes, not merely what is stored. Canonical form is hand-written JSON: sorted keys, no whitespace, fixed-width decimals, never scientific notation. | Closes `RECORD_SCHEMA.md` question 4, which required an answer before the first fixture was recorded. Quantizing at the boundary sidesteps the round-trip problem rather than solving it: nothing depends on a double surviving a decimal round trip, so neither hex floats nor 17-digit strings are needed and a record still fits in a chat message. `JSON.stringify` was rejected because its key order follows insertion and its float width follows engine precision, and the bytes are what gets hashed, so determinism must be a property of the encoder rather than of the caller. Asserted by round-tripping 2000 seeded values at exact equality. | Active |
| 013 | 2026-09-09 | `after.hash` is SHA-256 over the canonical encoding of `after.ball` and `after.events`, and nothing else. The inputs `seed`, `before` and `game` are excluded. | Closes `RECORD_SCHEMA.md` question 3. Hashing the inputs would make the digest move for reasons that are not the stroke outcome, and a fork, which replays one before-state with a different intent, could then never be compared against its parent. | Active |
| 014 | 2026-09-09 | The first run is **wordless**, and the eight-glyph vocabulary is **rejected**. Teaching falls through three drawn layers: world, then ribbon, then ghost. The onboarding specification is not written and its citations are removed. Supersedes the six pending first-run proposals. | The wordless principle is ADR-004 followed through, and it is kept. The glyphs are rejected on evidence rather than on taste: the intro hole was built without them and teaches power, curve and the putt through ripple rings, beacons, a ghost hand and a coloured trajectory stub. The vocabulary would buy nothing already missing while adding an authoring cost, a recurring ninth-glyph scope argument, and a sprites-versus-font decision. Nothing typed also means nothing to translate. | Active |
| 015 | 2026-09-09 | The intro hole's defender is an **archer that acts only on balls whose flight would carry them out of bounds**, pinning them where the arrow lands, with no stroke penalty. Skeet remains the M2 defender and the 1.0 roster is unchanged. | The intro hole is the one hole that must not be failable. A beginner’s characteristic disaster is spraying the ball off the course, and an out-of-bounds penalty is the harshest rule in golf arriving first. An archer that stops exactly those shots turns the defender beat into a safety net, which is the same principle as choosing the lesson from the lie rather than from a step counter. It is also unusually legible: it never acts on a good shot, so when it does act the player learns what out of bounds means without being told. This departs from the framing of defenders as pure adversaries, deliberately and only on this hole — Pillar 6, kind comedy. | Active — the archer stands; the par-4 hole it guarded is superseded by ADR-017 |
| 016 | 2026-09-09 | The intro hole is rendered as a **holodeck blueprint**: a solid dark-grey deck, a cyan grid, glowing outlines, an amber course boundary drawn where it actually is, and the ball as the one lit solid object. No depicted golf course. The archer stands on top of the spire. Scenery reuses the existing signal palette rather than introducing one. | Two reasons, and the second is the load-bearing one. **Tone:** the sunlit manicured fairway sells an image of golf, and the irrigation behind it, that this project does not want to promote; a simulation that is openly a simulation says what the game actually is. **Readability:** the hole already taught through geometry, a coloured stub and a ghost hand, so it was a diagram wearing grass, and the grass was actively in the way — the archer of ADR-015 enforces a boundary the player had no way to see, and on a deck that boundary is simply the edge of the deck. Drawing it also removed the trees, which existed to hide a world edge that is now the thing worth showing. Cheap as well: the whole deck grid is fewer vertices than one of the trees it replaced. Scoped to the intro hole; whether the biomes adopt it, contrast with it, or are reached through it is left open. | Active |
| 017 | 2026-09-09 | **Three clubs and an auto-putter are ratified**, as `ClubProfile` resources: driver, iron, wedge, putter, differing by launch speed band, launch angle and curve. The first run becomes a **private practice range** with three pins at three distances, one per club, replacing the par-4 hole "The Handshake". Supersedes the intro-hole half of ADR-014 and ADR-015; the archer, the wordless rule and the blueprint look all carry over unchanged. | The two decisions are the same decision. Until now `BallFlight` held one set of numbers for every full shot, so every stroke in the game was the same club wearing whichever name the record happened to store -- three clubs could not be *taught* because they did not exist. A range is how anybody has ever learned what a club does: hit it at something, watch where it goes. It also puts the first screen squarely on M1's gate, *is it fun to hit balls at nothing, on a phone?*, where the par-4 answered a question from a later milestone. The Handshake taught power, shaping and the putt with one club and a lie-driven lesson machine; that was good work for a hole and the wrong first thing to show. Nothing is scored against par on a range, so the mode also drops the one part of the old hole that could make a beginner feel behind. | Active — the range stands; its club set is superseded by ADR-018 |
| 018 | 2026-09-09 | The club set is **long, short and putt**, and the **player chooses**, from a selector along the bottom of the screen that draws each club as a bar the length of its reach. The pin suggests a club when it comes up and never imposes one. Supersedes ADR-017's driver/iron/wedge/putter and its automatic putter; `intent.club` becomes `long \| short \| putt`. | Two simplifications, both about the same barrier. Golf's own vocabulary carries golf's own cost of entry: driver/iron/wedge asks a player to already know that a wedge is the short one, which is exactly the knowledge a first run cannot assume. Long, short and putt say the decision rather than the equipment, and there are three because three is how many decisions there are -- a fourth club would be a distinction without one. Making the putter selectable rather than automatic follows: an auto-putter is a rule the player has to learn is being applied to them, and on a range there is no green to trigger it. The selector is the game's only control and still has no words on it, because a bar as long as the club is far is legible faster than the word "wedge" is readable. Presses inside it are consumed by the Control, so a tap on it cannot start a stroke -- no rectangle checks in the gesture. | Active — the club set and the player's choice stand; the selector's placement is superseded by ADR-019 |
| 019 | 2026-09-09 | The club selector moves to the **top-left corner**, gets much quieter, and draws each club as **the shot it hits** rather than as a bar: the putt is a flat line because it rolls, the short club a small steep arc, the long club a long shallow one, all on a small black panel local to the corner. Its rows stay a fingertip tall. Supersedes ADR-018's "along the bottom of the screen" and its "bar the length of its reach"; the wordlessness, the three clubs and the player's choice are untouched. | The bottom strip was legible and it was the loudest thing on the screen, which is the wrong ranking: the range is what is being looked at and the club in hand is a note in the margin. Ranked the other way round it reads as a menu to be dealt with before playing, which is the barrier Pillar 3 exists to remove -- and the one full-width band the game has was also the strip the scorecard wanted and a thumb rests on. The corner is where a note in the margin goes, and it is the one part of the frame nothing else draws into. Subtle is a claim about *ink* and not about the target: ADR-007 keeps touch the reference input, so the rows stayed a fingertip tall while only what is drawn inside them got quiet, and `test_club_selector.gd` now asserts both halves so that neither can be traded for the other later. The arcs are the same argument applied to what is drawn: a bar's length said how far a club goes and threw away *shape*, which is the difference a beginner meets first -- needing to get a ball over something has no reading at all on a horizontal line. Both the span and the height come from the club's own `carry()` and `launch_deg`, so the picture cannot drift from the ball. The black panel is the one opaque thing the game draws, and it is there because the first corner draft was quiet enough to be unreadable over a lit deck: contrast bought locally, rather than by putting a bar back across the frame. | Active |
| 020 | 2026-09-09 | On the practice range a defender's position is **derived, not authored**: it stands at twice the distance to the pin, mirrored through it, clamped inside the boundary. The golfer gets a **tell of its own** -- a backswing during which the ball is genuinely held -- and the first screen carries a **switch between golfing and defending**, both sides playing the same range from the same camera with the same overlay. A defender played by hand acts on the press rather than on a prediction, and whether it connects is **deterministic**. | Pillar 5 has said since the first draft that defence is a whole way to play and a pure defender never has to swing, and there has never been a way to do it: §4 puts Defense Range and Pass-and-play at M5, and modes are expensive. This is the smallest thing that answers the question the pillar poses, and building it turned up two things that were missing rather than unbuilt. **The golfer had no tell.** §3 requires every defender to telegraph; the symmetric requirement was implied and absent, and a defender who learns a shot is coming by watching the ball already leave has been given nothing to read. **And placement was a level-design decision for a mode that has no level designer.** Deriving it from the lie makes the defender a property of the shot: always on the line of play, never walk-around-able, and moving every lie, which is ADR-009's one-defender-per-lie with the choice taken out of anybody's hands. Twice the distance rather than half: halving put an archer at the player's elbow on a six-metre pin, against §3's own "defenders never enter the tee box", and a short putt ought to be the easiest thing a first run ever asks for. Mirroring also changes what the defender is *for* -- it guards the ground beyond the target, so going long is the mistake it punishes, which is the one mistake a range cannot otherwise charge anybody for. The defender is an **archer** and not the skeet: ADR-015 keeps skeet for M2 and putting it here would settle that by accident, while archery is the sport §3 already gives two jobs, so this needed no new sport and no new brain -- `ArcherBrain` falls back to the apex trigger by itself once it is not guarding a boundary. **This conflicts with §11.4**, which proposes that a human defender authors a plan rather than steering in real time, and that proposal is unratified. The property §11.4 exists to protect is replayability, and it survives: the action is deterministic, and its time is one scalar, so a hand-played defence is recordable. Whether the plan model replaces this at M5 or wraps it is the ratifier's call and is not settled here. | Active — the switch, the derived placement and the golfer's tell stand; the shared camera and the press-to-fire are superseded by ADR-021, and the contesting archer is scoped out of the first run by ADR-022 |
| 021 | 2026-09-09 | Defending is played **third person over the defender's own figure**, and with **the same pull-aim-release gesture as the stroke**. The archer's arrow becomes a real projectile: launched through `BallFlight` from a bow profile, previewed by the same `AimRibbon` the golfer uses, and connecting on where it actually arrives. Supersedes ADR-020's shared camera and its press-to-fire. | Two corrections to ADR-020, and both are the same correction. **The camera.** ADR-020 gave the defender the golfer's view on the grounds that both sides should see the same thing, and that was the wrong reading of it: Pillar 5 says defence is a *whole way to play*, and a whole way to play does not get somebody else's viewpoint. What has to be equal is that neither side gets a god view, not that they share one camera between them. It is also unplayable as specified -- a lead is a direction, and a direction cannot be judged from a camera pointed the other way. **The input.** Pillar 1 says golf is always the answer and never a separate minigame; the symmetric claim, which Pillar 5 implies and nobody had written down, is that defending must not be a separate *control scheme* either. A tap is a different game played with the same fingers. The same drag, meaning draw instead of swing, is the same game from the other end -- and it is learnable by anybody who has already learned the stroke, which is the only onboarding a wordless game can afford (ADR-014). Making the arrow travel follows from the gesture rather than being added to it: §3's counter for archery is literally "arrows have travel time", and a press that resolved on arrival threw that away. The AI's arrow stays a tracer for the reason its own comment gives -- its pin lands on the tick it acts, so a projectile would arrive after the ball had stopped, and cause after effect reads as a glitch. A hand-played arrow inverts that exactly: nothing has happened when the player lets go, so the travel *is* the anticipation. Still deterministic, so a hand-played defence still replays: the arrow is integrated on the physics tick alongside the ball and connects on proximity plus the same blind-spot test, with no dice anywhere. | Active |
| 022 | 2026-09-09 | The first run has **exactly one defender: the archer on the rock**, and the player can pick it up. ADR-020's contesting archer is scoped out of the tutorial and kept for later holes, behind a `set_contested()` switch that builds and frees it. Switching sides on the first screen hands the player the safety net itself, and while it is held it **stops guarding on its own**. | A beginner meeting an adversary on the same screen as their first swing meets two ideas at once, and ADR-014 and ADR-017 make the first run about the clubs. One defender is the whole budget the tutorial has. Which one is then obvious: ADR-015's archer is already there, already the thing that makes the range unfailable, and already the most legible defender in the game because of what it does *not* do. Making it the holdable one is the part worth the ADR. It is a better first defence lesson than an adversary would be, because the ball you are asked to shoot is the one that was about to be lost -- so working that side is learning where the course ends by patrolling it, which is the same lesson the golfing side teaches from the other end and Pillar 6's kind comedy either way. The net going quiet while somebody holds it follows: a net that keeps catching balls while the player is aiming it themselves is doing their job for them, and a shot they just missed would read as one they made. It costs nothing to be wrong -- a range charges nothing for a lost ball -- but it is the difference between watching a safety net work and being one. Supersedes the first-run half of ADR-020; the placement rule, the golfer's tell and the switch itself are unchanged. | Active |
| 023 | 2026-09-09 | Defending, the player supplies the **bearing, the draw and the timing**; the archer supplies the **elevation**, solved ballistically against where the ball will be. The aim ribbon runs much longer on this side than the golfer's stub, and the arrow is tested against the segment it swept rather than the point it reached. | ADR-021 made the defence the same gesture as the stroke, and the stroke's gesture reads a heading on the **ground plane** — a club supplies the launch angle, so the drag only ever had to supply a compass bearing. An arrow has no club to supply it and the target is in the air, so a planar aim with a fixed launch angle can only hit a ball that happens to be at the right height at the right range. That is not a hard shot, it is an unaimable one, and the side was decorative until this was fixed. Solving the elevation keeps the half of the skill worth keeping: the horizontal lead and the moment are the player's, and both are things they can see, judge and get better at, while the angle to reach a given range is craft the archer plausibly has. The bearing is *not* assisted, so a lead that is wrong still misses, and misses by exactly how wrong it was. The longer ribbon follows from the same argument: a golfer is denied the landing point on purpose because judging distance is the game, and an archer sights — a bow whose line stops a metre past the arrow is a bow with no sights on it. The swept test is not a design decision but a bug this found: at a hundred metres a second an arrow crosses nearly two metres between physics ticks, so a point test steps over a ball it went through, which is the same reason the ball itself uses continuous collision. `test_practice_range.gd` now proves the side is playable in both directions — a correct lead connects, and shooting at where the ball *is* misses. | Active |
| 024 | 2026-09-09 | Test builds are produced by `tools/build.sh` and nothing else. **`export_presets.cfg` becomes a tracked file**, with every keystore field blank and supplied at build time through the `GODOT_ANDROID_KEYSTORE_DEBUG_*` environment variables. The Android test build is the **prebuilt template APK** (no Gradle), arm64 only, declaring **no permissions**, at the provisional package id `org.golfvs.test`. Debug only: there is no release keystore and release signing is a separate decision. `project.godot` gains ETC2/ASTC VRAM compression, which Android refuses to export without. | Lane H asked for "export presets for Android and desktop, keystore path out of the repo", and the ignored presets file was answering the second half by giving up the first: it kept the *whole preset* out of the repo, not just the path. A build that cannot be reproduced from the repository is not a build, it is a souvenir, and ADR-002 makes GitHub releases canonical, which needs a preset a runner can read. Blank fields plus environment variables give both — nothing secret and nothing machine-local is committed, and the debug keystore's password has been "android" since 2008 in any case. The larger finding is that **M0's exit was not blocked on hardware**, or not mostly. `HANDOFF.md` has recorded it that way since the first session; what was actually in the way was three undocumented setup steps — export templates, a Java path, a keystore — and a setup step that is not in a script blocks the next person exactly as hard as it blocked the last one. What remains genuinely hardware is one line: someone installing the APK on a phone. `.test` is in the package id because Open Question 1 is still open and a package id change is an uninstall; a provisional build must not be mistakable for the real one. No permissions is a Pillar 4 property worth being able to point at rather than a default nobody checked, and `aapt2 dump badging` now says so. | Active |
| 025 | 2026-09-09 | **v0.0.1 is published as an openly labelled test build**, not a release: debug artifacts, a provisional package id, and a `CHANGELOG.md` whose *Not in it* and *Known gaps* sections are as prominent as its feature list. The project ships its own icon, drawn from the game's own palette and motifs; carries Godot's licence in `THIRDPARTY.md` because the engine is statically linked into both artifacts; and **excludes the workshop from every export** — gdUnit4, `tests/`, `tools/` and the docs. Android is sensor-landscape and declares no permissions. | Publishing is not the same act as releasing, and conflating them is how a pre-alpha ends up judged as a product. The README's pitch describes twelve sports and a course; what builds is one range, one defender and box art, and somebody arriving at a download must not be able to miss that — so the honest list is a section of the changelog rather than a footnote. Three of the fixes were not cosmetic. The **icon was Godot's logo**, which told anyone who saw it that the application *is* Godot and used the Godot Foundation's mark as this project's product identity. **Godot's licence travelled with nothing**, although the engine is most of both binaries and MIT requires the notice to accompany the distribution; `NOTICE` covered gdUnit4, which does not ship, and not the engine, which does. And the **export packed the entire test framework**: 2.1 MB of gdUnit4 against 355 KB of game, plus runnable dev probes. Excluding it took the data pack from 1.9 MB to 232 KB, which is the clearest possible statement of how much of what shipped was not the game. Sensor-landscape is also load-bearing rather than tidy: the project was landscape only because that is Godot's default, and Godot strips defaults on save, so the one thing the game most assumes about a phone was recorded nowhere. | Active |
| 026 | 2026-09-09 | **Aiming is a subsystem with correctness requirements, not a gesture tuned for one camera.** Three, and each is now a test: (1) the shot leaves opposite the drag *as seen on screen*, at every camera angle — the drag is unprojected onto the aim plane through the camera rather than mixed from its flattened basis; (2) the drag responds throughout, so the line only locks where there is a second phase to lock for — a stroke has curve, a bow does not; (3) every club is previewed in the terms it actually moves, so a putt is drawn as roll and not as a projectile. Plus a flat direction line in the aim plane, because perspective is what breaks legibility at a low angle. Raised at M1: the gate gains a precondition — *does the control do what it looks like it does?* | The first pre-alpha feedback arrived as three complaints and they were one failure. The aiming model was specified for a fixed camera, one club and one side, and everything built since reached outside it: ADR-001's orbit made every camera angle reachable, ADR-018 added a club that does not fly, and ADR-020/021 added a whole second side with no curve to bend. Each addition was individually sound and each quietly widened the domain of a function nobody had restated. **Measured, the drift was not subtle:** the old mapping was off by 2.8° at a steep camera and 32.9° at a shallow one, which is exactly the reported "works straight top down, stops responding at a lower angle". The lock was worse than drift — a bow's line committed after 26 px and the rest of the drag did nothing at all, so the control had genuinely stopped responding. The reframing is the point rather than the three fixes: Pillar 2 says the player always knows *why*, and it had only ever been read as a rule about defenders. It applies first to the player's own aim, because a control that does not do what it looks like it does makes every other signal in the game unreadable. Feedback from playing is promoted to a first-class input alongside the suite and the demo, since none of this was findable from either — the tests that pin it now were written *from* the report, and they fail against the code that shipped. Rounds are logged in `PLAYTEST.md`, which is where that third input lives — the other two have `tests/` and `demo_round.gd`, and an input with nowhere to be recorded is not first-class. | Active — round 1's fixes confirmed by the reporter |
| 027 | 2026-09-11 | **CI is cross-platform, and a release is drafted by a machine and published by a person.** The gdUnit4 suite and the demo round run on Linux, Windows and macOS on every pull request; Windows and macOS are informative before they are required, and join the required-checks list only once each has passed. `tools/build.sh` produces **four debug targets** — Windows, Linux, macOS and Android — from one host, with `export_presets.cfg` gaining a Linux and a macOS preset; the macOS bundle is unsigned and un-notarized. **Revised:** CI builds **Windows, Linux and Android on every pull request** and on `main`, not only on a tag — M0's exit is an APK on a phone, and an APK that exists only when somebody tags is an APK nobody installs; macOS exports fine and stays a local target, `tools/build.sh macos`, because nobody on the project can open the result. A `v*` tag assembles those three into a **draft** GitHub release whose notes are lifted from `CHANGELOG.md`; publishing it is a human act. The version is **checked, not remembered**: `tools/check_version_consistency.py` refuses any tree — and any tag — where `project.godot`, every export preset and the changelog's latest heading disagree. Godot's per-OS install is one composite action, `.github/actions/setup-godot`, reading the pin from `.godot-version` in exactly one place. Release signing is unchanged from ADR-025: still absent, still its own decision. | Lane H had three of these as open items and `RELEASE.md` carried the rest as a checklist. The matrix is the one that matters, and *corrected a week later* it is the precondition for the measurement rather than the measurement: DESIGN.md §11 advertises Stroke Records as portable and `RELEASE.md` has listed cross-platform determinism as unmeasured since M0, but each leg plays a round on its own random seed and checks only that it agrees with itself, so nothing yet compares a Linux hash with a Windows one. What the extra legs buy today is that the suite runs and the files land on operating systems the developer does not use. The measurement proper — one fixed seed, a digest per leg, a job that diffs the three — is Lane H's next task, and those legs stay off the required list until it exists. Linux and macOS cost one preset each and no new tooling, exactly as `RELEASE.md` predicted; all three desktop packs come out byte-identical — 234,588 bytes as measured, before ADR-028's splash added 98 KB to each — which is the exclusion filter proving itself. The draft is the shape that respects `RELEASE.md`'s *nothing here is automated on purpose*: the judgements it names — does the icon look like ours, does the pitch oversell the box, does it start on a real machine — are untouched, and only the assembling is taken off the person. The version check is ADR-006's move applied to a number: a checklist item is a hope, and four files that are supposed to agree have never had anything confirming that they do. macOS is un-notarized because there is no Apple identity and buying one to ship a debug build would decide the release question sideways; Gatekeeper's quarantine is documented in `RELEASE.md` rather than worked around. | Active |
| 028 | 2026-09-11 | **The first run opens on defence.** `main_menu.tscn` sets `start_defending` on the range it instantiates, so a new player's first frame is the game's golfer addressing a ball and a bow in their own hands; the side switch (ADR-020) still lets them take the club. The range as a component keeps the golfer as its base case — the flag defaults off, and the demo round and every existing test still read it that way. Opening on defence goes through `set_defending` like any other switch, and that switch now leaves `ATTRACT` for `AIM` when the bow is taken, because the game's golfer only ever swings in `AIM`. The loading screen is the project's own icon on the deck's black rather than Godot's logo: `boot_splash/image` in `project.godot`, a PNG because Godot accepts nothing else there, rendered from `icon.svg` by `tools/make_icons.gd` and committed outside LFS. | Ratifier's call, taken directly. Pillar 5 says defence is a whole way to play, and until now it was a way to play that had to be found behind a switch; a first run that *starts* there makes the claim on the first frame instead of the second. Which side a tutorial opens on is the tutorial's decision, so it is made in the scene that owns the first run and not in the range — a range that woke up on defence would have made the demo round and forty-odd tests lie. The `ATTRACT` fix is the useful thing this turned up: switching to defence on the attract screen had always left the player holding a bow at a golfer who never addresses the ball, because the only drag that leaves `ATTRACT` is the one the defending side ignores. It was reachable from the switch since ADR-020 and nobody had reached it; flipping the default made it the first thing every player would meet, and three tests now go red without the one-line fix. The splash is ADR-025's icon argument applied to the one place it had not been: a loading screen showing Godot's mark says the application is Godot. PNG is forced by the engine, verbatim — *the only supported format is PNG* — so it is the single PNG in the tree, exempted from LFS the way `addons/` is, because a pointer file where Godot expects an image is Godot's logo back on every fresh clone. | Active |
| 029 | 2026-09-11 | **The range never ends, the next pin is a digit of π, and an idle player is shown the range.** `State.DONE` is gone from the practice range: making a pin brings up the next one, forever. The order is the ternary expansion of π after the point — `PIN_ORDER`, 360 digits, wrapping — so the range plays putt, short, putt, long, short, short, … and the same three never come up twice running. Every `PINS.size()` pins made is a round: **its strokes are written to disk as a record file if a person struck a ball in it**, the list starts again, and `finished` fires; `finished` keeps its name and stops meaning that anything has stopped. The end-of-round orbit camera becomes the **idle camera**: after `IDLE_AFTER` seconds without a touch — a drag, an orbit, a tap, a club, a switch — the camera leaves the state's framing and circles the line of play, centred between ball and pin and sized to the distance, starting from the bearing it already had. Any touch brings it back. The clock only runs while the orbit is centred. | Ratifier's call: *the golfer just keeps golfing, picking holes following π in ternary; idle player should mean orbity camera.* Three things fell out of taking it literally. **Something has to choose the next pin forever, and it must not be a die.** A random choice would be the one thing on this range a record could not replay; π is deterministic, non-repeating, balanced across the three digits (104/130/126 in the first 360), and needs no seed to carry between sessions. Its first digit is 0, so a beginner still meets the putt first as ADR-017 asked; what ADR-017 gave up is only that the long pin now arrives fourth rather than third. **A round is a file, not a stop.** The record spine (§11, ADR-012, ADR-013) needs a unit to write, and three pins made is still that unit. *Revised the same week:* the first cut wrote the whole session every round so the demo's disk check could compare against one list, and an adversarial pass did the arithmetic — quadratic growth, roughly 43 MB an hour and 2.7 GB overnight from a first run left on a phone, because the game makes a pin every seven seconds against nobody. A file now holds its own round, the list clears, `last_round` keeps what was written for anyone checking the file, and a round in which no person struck a ball is not written at all; the demo remembers what it played. Measured: sixty seconds of unattended first run, ten strokes, nine pins, zero files. **Idle is a fact about the player, not the state.** The old `DONE` camera already orbited the range when nothing was left to do; now nothing is ever left to do, so it orbits whenever nobody is doing anything, and it starts from where the eye is so that leaving is a departure and not a cut. Three seconds is longer than any pause inside a stroke and shorter than the game's golfer takes to address and play one. Found on the way: GDScript lambdas capture by value, and the demo's first `done` flag was set inside one and read as false outside it — it played fourteen strokes looking for an ending it had been told about. It is a member now, and the comment says why. | Active |
| 030 | 2026-09-11 | **The camera turns about the player, the defender's view is pulled back and seven degrees right, and the idle tour is a blend and not a mode.** `_framed()` takes a pivot as well as a look-at, and the pivot is always the active player — `held()` under a bow, the ball under a club — so both the two-finger orbit and the idle tour swing around whoever is playing rather than a point down the line. `_frame_defend` stands a third further back and `DEFEND_YAW` (7°) round to the right of the archer, an angle rather than a lateral offset. The tour of ADR-029 is no longer a second framing: it is the state's own framing turned by `_tour_yaw` and pulled back by `IDLE_PULL_BACK`, raised by `IDLE_RISE`, with the look-at sliding onto the player, all scaled by a smoothstepped `_idle` that fades in over `IDLE_FADE_IN` and out over `IDLE_FADE_OUT`; the turn's rate follows the same blend and its unwind is rate-capped and proportional near zero. At zero it is the framing, exactly. | Ratifier: *orbit always around the active player; offset the angle of the defender when being played so the camera is zoomed out and 7 degrees right; demo to inactive mode should always be smooth, no jumps or sudden mode shifts, just input or not.* The pivot was the real finding: `CameraOrbit.apply` swings about the focus it is given, and every framing gave it the point it was looking at, which for the defender was thirty metres down the line — so a player orbiting was standing at the edge of their own orbit. Separating pivot from look-at fixes the manual orbit and the tour with one argument. *No jumps* was taken as a property to construct rather than a bound to tune: a second camera eased into is still a cut, only blurred, so the tour became a modification of the framing in force, and starts and ends where the eye already is because it is a turn from the view and not a view of its own. Then the same failure one derivative down — a linear ramp steps from rest to five metres a second in one frame — so the blend is eased and everything the eye does is keyed off the eased value. Measured at sixty frames a second on the defender's camera: onset 1.1 m/s, fade-in peak 6.7 m/s, touring 4.9 m/s, return peak 17.8 m/s, and no frame over 0.30 m; `test_the_tour_never_jumps` holds the bound at 1.0 m per frame for both the target and the eased eye. The return is brisker than the departure on purpose: the player touched because they want to act. Seven degrees and "pulled back" are the ratifier's numbers; the third is a judgement and is one constant. | Active |

## Pending ratification (tracked in `DESIGN.md` as `[PROPOSED]`)
Stroke gesture · blocked/caught penalty model · scorecard-only 1.0 · MVP four defenders · modes 1–11 for 1.0 · Scottish Rules par check as hole sign-off · async-only, no live play · vertex-color pipeline · GDScript + gdUnit4 + determinism · analytic preview · Stroke Record schema v1 · DefensePlan as human defense · commit-reveal protocol · PGN-style notation

**First run** — settled by ADR-014 and ADR-015, and no longer pending. The milestone rows in §8 stand:
the ghost-gesture demo at M1, the intro hole at M3 with the external playtest as its gate.
